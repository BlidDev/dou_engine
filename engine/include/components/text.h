#pragma once
#include <espch.h>

namespace engine {
    /*
     *
    struct TextComp {
        TextComp() {
            body = "";
            font_size = 13;
            color = BLACK;
        } 
        TextComp(std::string body, int font_size, Color color) {
            this->body = body;
            this->font_size = font_size;
            this->color = color;
        }
        std::string body;
        int font_size;
        Color color;
    };
    */

}
