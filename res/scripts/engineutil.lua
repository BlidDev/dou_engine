function v3_str(v)
    formatted = string.format("(%f, %f, %f)", v.x, v.y, v.z)
    return formatted
end

-- This is a desc
function color_str(c)
    formatted = string.format("(%f, %f, %f)", c.r,c.g,c.g)
    return formatted
end


function get_transfrom(s,u) 
    log_error("bru")
end
log_tace = nil
scene = nil
this = nil
